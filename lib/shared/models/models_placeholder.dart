// Placeholder for shared models.
// This directory contains data models shared across different modules (e.g., User, CommonSession, ErrorResponse models).

const String sharedModelsPlaceholder = "Shared Models Layer";
